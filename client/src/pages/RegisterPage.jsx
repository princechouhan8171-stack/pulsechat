import React,{useState}from'react';import{useNavigate,Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';export default()=>{const nav=useNavigate(),{register}=useAuth(),[f,setF]=useState({name:'',username:'',email:'',password:''}),[error,setE]=useState('');return <form className="max-w-md mx-auto mt-20 p-8" onSubmit={async e=>{e.preventDefault();const r=await register({...f,confirmPassword:f.password});r.success?nav('/chat'):setE(r.message)}}><h1 className="text-2xl font-bold">Create account</h1>{error&&<p className="text-red-500">{error}</p>}{Object.keys(f).map(k=><input key={k} className="w-full p-3 mt-3 bg-gray-100 rounded" type={k==='password'?'password':'text'} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button className="w-full mt-4 p-3 bg-indigo-600 text-white rounded">Register</button><Link className="text-indigo-600 text-sm" to="/login">Sign in</Link></form>}